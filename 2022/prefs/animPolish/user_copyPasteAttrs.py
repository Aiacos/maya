import maya.cmds as cmds

sel = cmds.ls (sl = 1)
if sel:
    for i in sel:

        try:
            cmds.setAttr ('{}.general_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.boxers_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.brows_flat_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.buttons_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.cornea_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_hue'.format (i), 247.800003052)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_brightness'.format (i), 0.300000011921)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_saturation'.format (i), 0.5)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_specBrightness'.format (i), 0.699999988079)
        except:
            pass

        try:
            cmds.setAttr ('{}.facialHair_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.frames_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.framesStud_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glass_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.glassEdge_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_hue'.format (i), 247.800003052)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_brightness'.format (i), 0.20000000298)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_saturation'.format (i), 0.5)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_specBrightness'.format (i), 0.699999988079)
        except:
            pass

        try:
            cmds.setAttr ('{}.hair_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitPrimary_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitSecondary_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.heroSuitTertiary_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.iris_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lacesRings_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_brightness'.format (i), 0.40000000596)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_saturation'.format (i), 1.25)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.lips_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_brightness'.format (i), 0.800000011921)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.nails_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_colorize'.format (i), 0.40000000596)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_hue'.format (i), 81.8000030518)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_brightness'.format (i), 1.5)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_saturation'.format (i), 0.5)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.pants_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_colorize'.format (i), 0.600000023842)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_hue'.format (i), 319.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_brightness'.format (i), 0.5)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_saturation'.format (i), 1.25)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shirt_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoes_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.shoesAccent_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_brightness'.format (i), 0.599999964237)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_saturation'.format (i), 1.25)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinBody_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_brightness'.format (i), 0.599999964237)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_saturation'.format (i), 1.25)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.skinHead_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.socks_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.teeth_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tie_specSaturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_label'.format (i), 0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_colorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_hue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_brightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_saturation'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_specColorize'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_specHue'.format (i), 0.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_specBrightness'.format (i), 1.0)
        except:
            pass

        try:
            cmds.setAttr ('{}.tongueGums_specSaturation'.format (i), 1.0)
        except:
            pass

